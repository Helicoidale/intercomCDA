{% extends 'base.html.twig' %}

{% block title %}modifier mon profil | {{ parent() }} {% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 50%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h3> profil de : {{ user.username }} </h3>
  <p>modification du profil </p>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Modifier !</button>
        {{ form_end(form) }}


    </div>
{% endblock %}
