{% extends 'base.html.twig' %}

{% block title %}Personne{% endblock %}

{% block body %}
    <h1>Personne</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Nom</th>
                <td>{{ personne.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ personne.prenom }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ personne.mail }}</td>
            </tr>
            <tr>
                <th>Fonction</th>
                <td>{{ personne.fonction }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('personne_index') }}">retour à la liste</a>

    <a class="btn btn-primary" href="{{ path('personne_edit', {'id': personne.id}) }}">modifier</a>

    {{ include('personne/_delete_form.html.twig') }}
{% endblock %}
